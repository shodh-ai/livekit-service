# livekit-service/requirements.txt
# Minimal requirements for fast Docker builds and basic functionality
# Use this for development/testing when you don't need full ML capabilities

# Core web framework
fastapi
uvicorn
python-dotenv

# LiveKit core components (essential for agents)
livekit
livekit-protocol==1.0.2
livekit-agents[deepgram,silero]~=1.0

# Essential utilities
aiohttp==3.11.18
grpcio
grpcio-tools
click==8.1.8
asyncio
livekit.plugins.turn_detector 
# Pydantic for data validation
pydantic==2.10.5

# Basic LangChain for AI functionality (minimal set)
langchain-core==0.3.60
langchain==0.3.25

# Protobuf for RPC communication
protobuf>=5.0.0